const fs=require('fs'),path=require('path'),prettier=require('prettier');(async()=>{let count=0;for(const file of fs.readdirSync('copia-dronex/assets/css')){const p=path.join('copia-dronex/assets/css',file);try{const css=await prettier.format(fs.readFileSync(p,'utf8'),{parser:'css',useTabs:true,printWidth:110});fs.writeFileSync(p,css);count++}catch{console.log('CSS original conservado:',file)}}console.log('CSS formateados:',count);})()
